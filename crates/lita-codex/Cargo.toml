[package]
name = "lita-codex"
version = "0.0.0"
description = "Drives the user's Codex CLI to produce structured text for Lita."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
