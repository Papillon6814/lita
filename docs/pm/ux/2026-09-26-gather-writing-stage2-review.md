# #129 段 2「その場で足す」実装後のレビュー（PR #134）

- 作成日: 2026-09-26（lita-ux）
- 対象: PR #134（ブランチ `feat/129-slack-entry`、コミット d54a8fa）
- 照らしたもの: 要件 `docs/pm/requirements/2026-09-26-gather-writing.md` の段 2（必須 1〜6・13〜15、受け入れ条件 1・2・7）、設計書 `2026-09-26-gather-writing-stage2-4.md`（3.1 節、場面 ①〜⑥）、実装報告 `docs/pm/dev/2026-09-26-gather-writing-stage2.md`、`principles.md`
- 正として扱った本人の決定（2026-09-26）: 「文章を足す」は雲が出ないときだけ。雲が出たあとは置かない（設計書 9 節の推奨「残す」は不採用）

## 1．結論

**承認します。** 段 2 の受け入れ条件 1・2・7 と必須 1〜6・13〜15 は、モックの画ですべて満たしました。原則に反する指摘はありません。lita-dev が設計から外した 4 点はすべて妥当なので、設計書の側を直しました（4 節）。受け入れ条件 2 は、実物の 1,500 字と本物の Codex で雲が出るかがまだ確かめられていません。これは画面の話ではないので、マージの前後に lita-dev が 1 回確かめれば足ります。

## 2．判定表

合計: **満たす 11／満たさない 0**（うち受け入れ条件 2 は mock で満たし、実物の確認が残る）。

| # | 条件（要約） | 見たもの | 判定 |
| --- | --- | --- | --- |
| 受 1 | おすすめの文体だけ（文章 0・記事 0）で、雲の場所に次の一手が 1 つだけ。約束だけの一文は無い | `s2-topics-add-empty.png`：箱の中に一文と「文章を足す」が 1 行。主ボタンは右下の「題を出す」だけ | 満たす |
| 受 2 | 押すと画面を離れず貼る欄とつなぐ行が開き、足すと画面を離れず拾って雲が出る。「元にした文章」に 1 本 1 行、8 項目は変わらない | `s2-topics-add-open.png` → dev の `flow-3-gathering`・`flow-4-cloud`・`flow-5-voice`。学び直しは呼ばれていない（コード） | 満たす（mock。実物の確認は残る） |
| 受 7（段 2 の分） | 取り込みに失敗しても平易な一文、何も保存されず、「題を出す」は押せる | dev の `stage2-topics-add-failed-after-add.png`：区画は開いたまま、貼った文字が残り、「インターネットに接続できませんでした。…」。主ボタンは押せる | 満たす |
| 必 1 | 雲が出ないときだけ一手が 1 つ。主ボタンは「題を出す」のまま | A（0 本）・B（`s2-topics-cloud-few.png`）・C（`s2-topics-cloud-stopped.png`）とも箱の中は一文＋リンク 1 つ。雲が出たあと（`flow-4-cloud`、`s2-topics-cloud-more.png`）は見出しの右が「拾い直す」だけで、本人の決定どおり | 満たす |
| 必 2 | 画面を離れず、既存の部品がその場で開く。モーダルにしない。閉じれば戻る | 区画は雲の箱の場所に開き、「閉じる」と Esc で箱に戻る（dev の `flow-esc`、キーボードは「文章を足す」へ） | 満たす |
| 必 3 | 足す先は直近の記事の文体 | `voiceId`（D-55 の選び方）。文体名は `add.where` の一文に出る | 満たす |
| 必 4 | 文体は学び直さない | `addVoiceSources` だけを呼ぶ | 満たす |
| 必 5 | 足し終えたら自動で拾う。見せ方と中止は今の「拾う」と同じ | 足すと区画を閉じて `gather(true)`。拾っている間は「言葉を拾っています… やめる」だけで、一手と同時に出ない（段 1 レビューの宿題 2 も解消） | 満たす |
| 必 6 | 文章 0 本の文体でも同じ一手。記事だけの人も | `s2-topics-add-articles.png`：記事はあるが文章 0 本でも A と同じ（段 1 レビューの宿題 1 も解消） | 満たす |
| 必 13 | 数字を出さない | 新しい文言に数字なし。貼る欄の「2 本として読みます」は既存の表示 | 満たす |
| 必 14・15 | 失敗は平易な一文、「題を出す」は使える。画面の語は「文章」「元にした文章」「言葉」 | 受 7 と同じ。新しい 5 キーに「学習」「ソース」「材料」「データ」なし。ja/en は同じ意味で 5 対 5 | 満たす |

原則との照合: 主ボタンは各状態で 1 つ（原則 3）。区画の「足す」と「題を出す」の間には、つなぐ器が挟まって並ばない（原則 3）。一手は常に 1 つ（原則 6）。見出し行の位置は動かない（原則 2）。新しい色・角丸なし（原則 8、`.cloud-invite`・`.add-open`・`.add-why` は既存トークンだけ）。手数は設計どおりで、クリック 3→2・画面の移動 2→0・判断 2→1（原則 10）。

## 3．lita-dev が設計から外した 4 点

| # | 外した点 | 判定 | 理由 |
| --- | --- | --- | --- |
| 1 | 本人の決定に従い、雲が出たあとは「文章を足す」を置かない | 妥当 | 本人の決定が正。設計書を直した |
| 2 | 段 4 の 1 文（`paste.talkHint`）を入れない | 妥当 | 必須 17 は段 4。会話を見分ける作りが無いまま「チャットも貼れます」と出すと、約束だけの一文になる（必須 1） |
| 3 | キーは `cloud.add` 1 つ（`add.title` を作らない） | 妥当 | 一手と区画の見出しは同じ語（原則 1）。設計書に「1 つにしてもよい」と書いていた |
| 4 | 状態 C（止めた）を、下限未満の雲の拾い直しを止めたときにも広げた | 妥当 | 広げないと B の「文章を足す」と「新しい文章が増えています。拾い直す」が箱に 2 つ並ぶ（原則 6）。文章は増えているので「足す」より「拾い直す」が正しい一手で、一文「言葉を拾うのをやめました。」も事実どおり。「新しい文章が増えています」を言葉があるときだけにしたのも、段 1 までの見え方を変えない |

## 4．lita-dev の懸念 4 つ

| # | 懸念 | 判定 | 理由・扱い |
| --- | --- | --- | --- |
| 1 | 実物の 1,500 字と本物の Codex で試していない | 指摘にしない（画面の外） | 画面の流れはモックで確かめた。3 語に届かなければ B に戻るのは設計どおり。マージの前後に lita-dev が実物で 1 回確かめ、拾う時間と B に戻ったかを PR に書けば足りる |
| 2 | 区画を閉じても、進んでいる note などの取り込みは止まらない | 許容 | 「つなぐ」は本人が押した操作で、閉じるのは画面を戻すため。取り込みが終わると箱に「言葉を拾っています… やめる」が出るので、何が起きたかは見え、止める道もある。失うものが無い。ただし、取り込み中に「題を出す」を押して題が出たあとに取り込みが終わると、自動の拾いで押した言葉が外れる（`gather(true)` が `subjects` を空にする）。起きるのは稀で、原則には反しないので、直すかは lita-dev の判断に任せる |
| 3 | 文体 2 つ以上・記事 0 本だと、足す先が一覧の先頭 | 許容 | D-55 はこの場合を決めていない。足す先の名前は `add.where` の一文に出て、画面の文体の選択を変えれば足す先も一緒に変わる（`voiceId` に従う）。想定は 1〜2 本・数件（原則 6）で、選ぶ部品を足すほどではない |
| 4 | mock 場面 `topics-add-failed` を足した | 妥当 | 受け入れ条件 7 を画で確かめるのに要る。設計書 7.3 に無かったのは設計の漏れ |

## 5．画（PNG）

レビューで撮ったもの（`npm run mock`、1120×900、`--lang=ja`）: `docs/pm/ux/gather-writing/review/`

- `s2-topics-add-empty.png`（A: 文章 0 本）
- `s2-topics-add-open.png`（区画が開いた）
- `s2-topics-add-failed.png`（失敗の場面を開いた直後。貼る前）
- `s2-topics-add-articles.png`（記事だけで文章 0 本）
- `s2-topics-cloud-few.png`（B: 言葉が 3 つ未満）
- `s2-topics-cloud-stopped.png`（C: 最初の拾いを止めた）
- `s2-topics-cloud-first.png`・`s2-topics-cloud-few-more.png`・`s2-topics-cloud-more.png`（拾っている・雲が出た。回帰の確認）

貼る・足す・失敗・Esc の流れと英語の画は、lita-dev の `docs/pm/ux/gather-writing/impl/stage2-*.png` を目で見て判定した（静止画の撮影では貼れないため）。英語の画は `?lang=en` が効かず（言語は `navigator.language` で決まる）、レビューでは撮り直していない。

## 6．指摘

なし（受け入れ条件・原則に反するものは無い）。

範囲外の気づき（この PR では直さない）: 文体ページの「元にした文章」の見出し行に「学び直していない材料があります。」「この材料で学び直す」が出ている（`flow-5-voice`）。「材料」は原則 1 の使わない語。既存の文言で、段 2 の必須 15（この画面の語）の外。別の機会に lita-requirements へ回す。

## 7．設計書の直し

`2026-09-26-gather-writing-stage2-4.md` を本人の決定と実装に合わせた: 冒頭に改訂の 1 行、3.1 節に「雲が出たあとは置かない」「`paste.talkHint` は段 4」、状態 C の条件の拡張、6 節のキーを `cloud.add` 1 つに、9 節の論点 1 を決定済みに。
