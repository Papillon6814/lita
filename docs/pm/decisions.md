# 決定ログ

> 論点・決定・理由の3点セットで記録します。理由のない決定は書きません。
> 覆った決定は削除せず「（YYYY-MM-DD 改訂）」と注記して残します。

## 2026-09-22 — 初期方針の確定

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-01 | 言語化の一次ソースをどこに置くか | **Notion を正、リポジトリは要約** | 仕様の議論は Notion の表現力（表・コールアウト）に乗せたほうが速い。リポジトリ側は外部コントリビューター向けの要約に徹する |
| D-02 | 誰のためのアプリか | **広報の発信者。X / note / Medium などの記事が中心** | 汎用エディタにすると差別化が消える。発信という具体的な行為に絞る |
| D-03 | 人間と AI の役割分担 | **AI が書く。人間はブリーフ入力と承認のみ** | 「一緒に書く」ツールは既に多い。AI が完成原稿を出し、人間は判断だけする体験に賭ける |
| D-04 | 文体をどう扱うか | **過去の文章から文体プロファイルを自動生成し、人間が編集できる形で保持する** | 体験の核。プロファイルは読める・直せるドキュメントとして扱い、ブラックボックスにしない |
| D-05 | 文体ソースの第一弾 | ~~**Slack（自分の過去発言）**~~ →**本人が選んで貼った文章**（2026-09-22 改訂、D-20 参照） | 広報担当が一番大量に自然な文章を書いている場所。記事 URL 取り込みは v0.2 |
| D-06 | Codex のログイン情報の使い方 | **Codex CLI をサブプロセス起動する。トークンは自前で扱わない** | `~/.codex/auth.json` を直読みする案は、トークン更新の自前実装と OpenAI 利用規約の両面でリスク。`codex exec` は保存済み認証をそのまま再利用するため、Lita は認証に一切触らずに済む。出典: https://developers.openai.com/codex/cli/reference |
| D-07 | GUI の技術スタック | **Tauri v2（Rust + Web フロント）** | サブプロセス起動・ファイルアクセス・カスタム URI スキームの権限制御が堂々としている。バイナリが軽くクロスプラットフォーム |
| D-08 | 保存形式 | ~~**SQLite 単一ファイル。認証情報は OS キーチェーン**~~ →**サーバが正。オンライン専用**（2026-09-22 改訂、D-36 参照） | 「ログインすれば別の PC でも引き継がれる」要件が出たため |
| D-09 | 投稿先連携の範囲 | **v0.1 は生成・エクスポートまで。投稿はしない** | X の API は有料、note に公式 API がない。投稿連携は Publisher プラグインとして後付けできる設計にしておく |
| D-10 | OSS 公開の方針 | **最初から public + MIT** | 開発過程ごと見せる。Codex トークンなど機密を絶対にコミットしない運用が前提（`.gitignore` で `auth.json` と `*.sqlite` を除外済み） |
| D-11 | UI とドキュメントの言語 | **英語をデフォルトにし、i18n の仕組みを最初から入れる** | OSS として世界に届けることを優先。日本語はロケールとして同梱する |
| D-12 | Slack 連携の実現方式 | **PKCE + `lita://oauth`**。ただし~~v0.1 から~~ →**v0.2 以降に延期**（2026-09-22 改訂、D-20 参照） | Slack の redirect_uri は通常 HTTPS 必須で localhost 不可だが、PKCE を有効にすると public client 扱いになりカスタム URI スキームが使える。client_secret が不要になるため OSS でソース公開しても成立する。中継サーバは不要。制約: bot scope 不可（user token のみ）、refresh token が30日で失効。出典: https://docs.slack.dev/authentication/using-pkce |
| D-13 | v0.1 の完成定義 | **1ボイス × 1プラットフォーム（X）で一周**（2026-09-22 改訂: 入口が Slack 連携から貼り付けに変わった。D-20 参照） | 体験の肝が成立するかを最短で検証する。マルチプラットフォーム・複数ボイス・履歴管理は v0.2 以降 |
| D-14 | リポジトリの公開先 | **`Papillon6814/lita`（個人アカウント）** | 個人プロジェクトとして軽く始める。必要になれば org へ移管する |
| D-15 | PM エージェントの運用 | **リポジトリ内にエージェント定義（`.claude/agents/lita-pm.md`）を置き、`docs/pm/` の状態ファイルをセッション開始時と終了時に自動更新する** | セッションを跨いでも文脈が復元でき、決定の再議論を防げる |

## 2026-09-22 — Codex 呼び出しの PoC を受けて

検証記録: [`docs/pm/2026-09-22-codex-poc.md`](2026-09-22-codex-poc.md)

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-16 | プリフライト（未インストール・未ログインの検出）をどう実装するか | **`codex doctor --json` を使う** | 機械可読レポートを返す。`checks["auth.credentials"].status` と `codexVersion` を読めばよく、人間向け出力のスクレイピングより安定する |
| D-17 | 生成中の進捗表示をどうするか | **ステージ表示のみ。本文のストリーミングは諦める** | `codex exec` にはイベントが4種類しかなく（`thread.started` / `turn.started` / `item.completed` / `turn.completed`）、delta 系フラグが存在しない。答えは最後に一括で届く |
| D-18 | JSON Schema の書き方 | **全フィールドに `description` を必ず書く** | 型だけでは意図が伝わらない。PoC で `first_person` に一人称ではなく段落が入った |
| D-19 | 約30秒の待ち時間をどう扱うか | ~~未決定~~ → **解決（2026-09-22、D-23〜D-25 参照）**。問題設定が間違っていた | 26.6秒は Voice プロファイル全文をプロンプトに入れた値だった。短いプロンプト＋推論量 low なら **6.6秒**。7秒ならインジケータで足りる |

## 2026-09-22 — Slack の権限要件の検証を受けて

検証記録: [`docs/pm/2026-09-22-slack-scopes.md`](2026-09-22-slack-scopes.md)

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-20 | v0.1 の Voice ソースをどうするか | **Slack API 依存をやめ、本人が選んで貼った文章にする。Slack ボタン連携は v0.2 以降に延期**（D-05 / D-12 / D-13 を改訂） | 理由は2つ。(1) **Slack 側がほぼ塞がっている** — `search.messages` は公式が名指しで非推奨、後継の RTS API は unlisted distributed app に非開放、`conversations.history` はレート制限以前に形が合わない（全員の発言が返る）。マニフェスト配布の抜け道も更新後の API 利用規約が名指しで塞いでいる。(2) **そもそも大量取得が要らなかった** — 実測で Voice は4件でほぼ安定し、24件との差は語彙の豊かさだけだった。数十件で足りるなら、本人が選んだ20件のほうが機械的に集めた500件より良い Voice になりうる |
| D-21 | v0.1 の完成定義（改訂版） | **気に入っている文章を20件ほど貼る → Voice を1つ生成 → ブリーフ入力 → X 用ポスト生成 → 承認 → クリップボードにコピー、が一周する** | D-13 の骨格（1ボイス × 1プラットフォーム）は変えず、入口だけを Slack 連携から貼り付けに差し替えた |
| D-22 | Slack 関連の未検証項目をいつ潰すか | **方針が固まってから。v0.1 では調べない** | 公開配布と `lita://oauth` の両立、`search:read` が今も取得可能か。いずれも Slack App の作成が必要で、PKCE の有効化は取り消せない。v0.2 で Slack に戻るときにまとめて確かめる |

## 2026-09-22 — 待ち時間の実測を受けて

検証記録: [`docs/pm/2026-09-22-latency.md`](2026-09-22-latency.md)

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-23 | 生成中の見せ方 | **チャット AI と同じ「生成中」インジケータ。文字は流さない** | Codex の stderr に進捗の中身はなく（確認済み、流れてきたのは無関係な MCP エラーのみ）、`--output-schema` を使う以上、答えは最後に一括で届く。**ただし短文生成は6.6秒まで落ちるので、埋めるべき無音がそもそもない** |
| D-24 | 推論量（reasoning effort） | **ユーザーが選べるようにする。速度重視 = low / 品質重視 = medium** | 短文では品質差が見えなかったが、長文で差が出る可能性を否定できない。こちらで固定せず本人に委ねる。`Effort` enum として実装済み |
| D-25 | ユーザーの Codex 設定の扱い | **`--ignore-user-config` を常時付ける** | 検証中、Lita の生成が本人の Notion MCP 設定を読み込んで OAuth エラーを出していた。生成結果や速度が他人の Codex 構成に左右されるのは避ける。**認証は `auth.json` にあり `config.toml` とは別なので、ログインは維持される**（実測確認済み） |
| D-26 | Voice 生成（45〜65秒）の見せ方 | **ここだけステージ表示を出す** | 初回セットアップの1回きりとはいえ、1分近い無音は重い。**ただし Codex はイベントを4種類しか返さないので、段階表示は演出であり実態ではない。承知のうえで採用する** |

## 2026-09-22 — Voice スキーマの確定

検証記録: [`docs/pm/2026-09-22-voice-schema.md`](2026-09-22-voice-schema.md)

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-27 | Voice スキーマ | **12フィールドで確定。`crates/lita-codex/src/voice.rs` が正** | 全フィールドに `description`（D-18）。ユニットテストで検証。`language` を追加し、生成プロンプトの言語をハードコードしない |
| D-28 | 引用（`representative_excerpts`）の扱い | **保存はするが、生成には渡さない** | 3回ずつの実測で、引用を渡すと `uses_emoji` の遵守が 0/3（渡さないと 2/3〜3/3）。さらに引用の文がそのまま出力に出た。具体例が抽象記述を上書きし、発信ツールでは事故になる。本人が「何から作られたか」を読む用途で保存だけする |
| D-29 | 生成に渡す形 | **`VoiceProfile::generation_view()` の出力のみ。構造化フィールド11個** | 特徴語の使用が最も多く（平均2.7語）、文の借用がなかった。`preferred_words` が個性を作る主因 |
| D-30 | 「プロンプトの大きさが待ち時間に直結する」（前回の主張） | **修正: 推論が有効なときだけ効く。速度重視モードでは気にしなくてよい** | 推論量 low では 317〜1270 文字で相関なし（6.9〜10.1秒）。前回の 10.8→26秒 は推論量 medium で、大きなプロンプトが推論トークンを増やしたのが原因 |

## 2026-09-22 — Tauri スキャフォールド

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-31 | フロントエンドの構成 | **React 19 + TypeScript + Vite。パッケージマネージャは npm** | `create-tauri-app` の `react-ts` テンプレートをそのまま土台にする。npm は本人の運用ルール（worktree 作成後に `npm install`）に合わせた |
| D-32 | ネイティブ側との境界 | **`@tauri-apps/*` を import してよいのは `src/platform/` だけ** | `invoke` を一か所に閉じ込めれば、将来ブラウザ版を出すときにモジュール1つの差し替えで済む。今は規約のみで、lint での強制は未設定 |
| D-33 | i18n の仕組み | **自前の `t()` と辞書ファイル。ライブラリは入れない** | v0.1 の文言は十数個。`Record<MessageKey, string>` 型で「全ロケールが全キーを持つ」ことをコンパイル時に保証できるので、この規模では十分 |
| D-34 | Tauri 側の責務 | **`src-tauri` は `#[tauri::command]` だけ。ロジックは `crates/` に置く** | `lita-codex` を path 依存で使う。UI から呼ぶ最初のコマンドは `codex_status`（`CodexCli::preflight()` を `spawn_blocking` で実行） |
| D-35 | バンドル識別子 | **`com.papillon6814.lita`**（仮） | 公開先が個人アカウントなので合わせた。配布前に変えるなら早いうちに |

## 2026-09-22 — 保存方式をサーバへ転換

調査記録: [`docs/pm/2026-09-22-backend-options.md`](2026-09-22-backend-options.md)（正は Notion 子ページ「バックエンド基盤の選定」）

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-36 | データの置き場所（D-08 を改訂） | **サーバが正。オンライン専用でローカル DB は持たない** | 「ログインすれば別の PC でも同じデータが使える」を優先。Codex のログインはストレージを伴わないので Lita 自身のアカウントが要る。ローカル優先＋同期は衝突処理が増えるため保留。SQLite の下書きは `feat/11-sqlite` に参考として残す（コンパイルは通らない） |
| D-37 | ログイン方式 | **Google OAuth。デスクトップではループバック（`http://127.0.0.1:port`）＋ PKCE** | Google 公式が Desktop app クライアントでループバックを推奨・継続サポートと明記。カスタム URI スキームは macOS の `tauri dev` や Windows で不安定なため使わない |
| D-38 | 同期対象 | **Voice・材料・ブリーフ・下書きの全部** | 生成履歴も全 PC で見えるようにする |
| D-39 | スコープ | **バックエンドとアカウントは v0.1 に含める** | 完成定義（D-21）の前段に「Google でログイン」が加わる |
| D-40 | バックエンド基盤 | **Supabase（東京 ap-northeast-1）** | 18 候補を調査。無料・東京・Google ログイン組み込み・セルフホスト可・素の Postgres を同時に満たすのは Supabase のみ。弱点は 2026 年の障害の多さで、RLS と SQL だけに依存して移設可能に保つことで担保する |

## 2026-09-22 — Google ログインの実機検証

検証記録: [`docs/pm/2026-09-22-google-login.md`](2026-09-22-google-login.md)

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-41 | Google が要求する `client_secret` の扱い | **アプリは Google と直接話さず、Supabase 経由の OAuth にする。Google 側は Web application クライアント、シークレットは Supabase が保持** | 実機で、Desktop app クライアントでもコード交換に `client_secret` が必須だと確定した。公開リポジトリにシークレットを載せない方を選ぶ。アプリは Supabase の `/auth/v1/authorize` に PKCE で入り、結果をループバック `http://127.0.0.1:<port>/` で受ける（許可リストに `http://127.0.0.1:*` 系を登録済み） |

## 2026-09-22 — 文体ソースの再検討

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-42 | Slack 連携 | **見送り（v0.2 の候補からも外し、凍結）** | Marketplace 掲載が唯一の正路だが、提出には既に 10 以上のアクティブなワークスペースへのインストールが必要（2026-07 に 5→10）、承認まで実例で 4 ヶ月、`search:read` は掲載不可、承認後はスコープ変更ごとに最大 6 週間の再審査。個人の OSS では前提条件を満たすのが現実的でない。技術面（Edge Function での OAuth、RTS API）は今の基盤で解けるので、状況が変われば再開できる |
| D-43 | v0.1 の文体ソース（D-20 を補強） | **本人の公開済みの発信から拾う。note（アカウント名から公開記事）→ Medium（同）→ X（公式アーカイブの取り込み）の順。貼り付けは受け皿として残す** | 「貼るのは億劫」という本人の実感と、再現したい文体は公開済みの投稿そのものにあるという理由。note は認証不要の公開 API、Medium は RSS、X は API の読み取りが有料のため公式アーカイブ（`tweets.js`）。いずれも審査や規約の壁がない。Codex / Claude Code のログや iMessage は「発信向けの文体ではない」ため見送り |

## 2026-09-23 — v0.2: 3 柱（文体・記事・エディタ）への再設計

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-44 | v0.2 の情報設計（D-13 / D-21 を改訂） | **アプリは「文体（Voice）」「記事」「エディタ」の 3 柱。ホームは記事一覧。サイドバー（記事 / 文体）＋メイン（一覧・エディタ・Voice）。エディタはプレーンテキストで長文（note / Medium）に対応。Voice は複数、記事ごとに選び、既定 Voice を持つ** | 本人の実感（2026-09-23）「文体調整・過去記事の管理・文章エディタの 3 つから成る」。参考は iA Writer / Ulysses の Library–Editor。配布（#25）より先にやる |
| D-45 | 記事と版のモデル | **`briefs` + `drafts` を廃止し、`articles`（title / body / brief / voice_id / platform_id / status: draft・approved・archived）と `article_versions`（kind: generated・shortened・edited・restored・manual、生成時は prompt_sent と elapsed_ms）に置き換え。生成は常に記事の本文を書き換え、版が増える。既存データは移行（brief 1 件＝記事 1 件、drafts＝generated 版）し、旧テーブルは drop** | v0.1 のモデルは「1 回の生成」しか表せず、編集し続ける文書を扱えない。利用者は本人のみなので移行して drop |
| D-46 | 自動保存と版の規則 | **自動保存は 600 ms の trailing debounce ＋ 5 s の max wait。表示は「保存しました（n 分前）/ 保存中 / 保存できませんでした」。失敗時はローカルに退避して再試行。版は生成・短く・復元・「版を残す」・編集中 10 分ごと・記事を閉じるとき（差分あり）に snapshot。同じ内容なら作らない。`edited` は記事あたり最新 50 件に間引く（DB trigger）。復元は元の状態を `edited` で残してから `restored` 版を作る（履歴は常に直線）** | 137foundry「autosave は debounce ＋ max wait、失敗状態を必ず出す」、uxpatternsguide「復元は新しい版を作る」 |

## 2026-09-23 — 配布（#25）

| # | 論点 | 決定 | 理由・補足 |
| --- | --- | --- | --- |
| D-47 | バンドル識別子（D-35 の「仮」を解消） | **`com.papillon6814.lita` で確定** | 署名・アップデーター・キーチェーン（セッションの service 名）に紐づくので、配布後は変えられない。個人アカウントのまま行く |
| D-48 | 配布経路 | **GitHub Releases が正。`v*` タグの push で Actions がビルドして公開し、アプリ内アップデーター（`releases/latest/download/latest.json`）と Homebrew cask（`Papillon6814/homebrew-lita`）はそれを読む。Apple Silicon のみ** | 置き場所を一つにして、更新の経路を増やさない。Intel は要望が出たら matrix に 1 行足す |
| D-49 | 公開の運用 | **タグを push したら自動で公開する（ドラフトにしない）** | 本人の選択（2026-09-23）。誤配布の歯止めはタグを打つ行為そのもの（`npm run release -- x.y.z` → `git push --tags`）。署名鍵: アップデーターの minisign 秘密鍵はキーチェーン `lita-tauri-updater-key` と GitHub Secret、公開鍵は `tauri.conf.json`。Apple の署名・公証は Developer Program 加入後に Secrets を足すだけで有効になる（workflow は無ければ ad-hoc） |
| D-50 | 更新の見せ方 | **起動時に静かに確認し、あればサイドバー下に「vX が利用できます」のピル。メニュー「Lita › アップデートを確認…」からはダイアログ。更新内容は GitHub の自動リリースノート** | 正常時は静か、の原則（2026-09-22 の UX 批評）。ダウンロード → 「再起動」で切り替わる |
| D-51 | Apple 署名・公証の時期 | **当面は本人用のみ（ad-hoc 署名の Release）。使い心地に自信が持てたら Apple Developer Program に加入して署名・公証を足す** | 本人の判断（2026-09-23）。作ること自体に Apple は不要で、必要なのは他人に配るときだけ。workflow は Secrets が揃えば自動で署名に切り替わる |
