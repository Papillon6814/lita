# v0.2 実装後の UX 再レビュー（2026-09-23、#49 段階 6）

> 3 柱（文体・記事・エディタ）を入れた状態で、`npm run mock` の全 19 場面を撮って前回と同じ基準（Nielsen、UI 原則、WCAG 2.2）で見直した記録。

## 見た場面

signed-out / codex-not-logged-in / intake / intake-loaded / building / voices / voice / voice-open / voice-trial-generating / voice-trial-compare / articles / articles-empty / articles-first-run / editor / editor-empty / editor-generating / editor-over / editor-note / editor-versions

## 指摘と対応

| # | 画面 | 指摘 | 原則 | 対応 |
| --- | --- | --- | --- | --- |
| 1 | 記事一覧（初回） | Voice がまだ無いのに「新しく書く」だけが出る。押すと Voice 未選択のエディタに落ちる | 10 ヘルプ・6 認識 | **Voice が無いときは「まず書き方を教える」と「文体（Voice）を作る」ボタン** |
| 2 | エディタ | Voice 未選択のヒントに行き先が無い | 3 制御 | **ヒントに「文体（Voice）を作る」リンク** |
| 3 | 取り込み | 固定バーの左右の余白が旧レイアウト（40px）のまま | 4 一貫性 | **36px に** |

問題なしと判断: サイドバーの現在地（aria-current）、記事行のボタン化、生成中の中止、版履歴の「いまの本文」印、試し書きの前／今、長文の読了目安、保存状態の三態。

## 残したもの

- 記事一覧の並び替え・タグ（今は更新順のみ）。本人の記事が 20 本を超えたら考える
- 試し書きのモックで「前／今」が同文になるのはモックの都合。実機では 2 回目の生成結果が入る
